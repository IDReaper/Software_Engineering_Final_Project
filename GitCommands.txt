Always do this first:
git pull
Once you have made changes to your file in the git repository
do the following commands:
git add .
git commit -m "Whatever you did"
git push

if you encounter error because you did not pull changes use this command:
  git stash
